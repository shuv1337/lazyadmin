# lazyadmin.snapshot.v1

Public JSON produced by `lazyadmin export --json`.

Required top-level fields: `schema_version`, `generated_at`, `host`, `managers`, `processes`, `listeners`, `workloads`, `projects`, `tracked_runs`, `edges`, `warnings`.

All arrays are present even when empty. `schema_version` is exactly `lazyadmin.snapshot.v1`.
